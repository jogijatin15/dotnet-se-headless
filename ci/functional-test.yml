platform: linux

image_resource:
  type: docker-image
  source: {repository: atin/dotnet-headless-chrome}

inputs:
- name: dotnet-se-headless

outputs:
- name: tests

run:
  path: sh
  args: ['./dotnet-se-headless/ci/scripts/functional-test.sh']
